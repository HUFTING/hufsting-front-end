import styled from 'styled-components';
import tw from 'twin.macro';

const ProfileHeaderSection = styled.section`
  & {
    ${tw`flex items-center justify-between mx-4`}
  }
`;

export default ProfileHeaderSection;
