import React from 'react';

const SignUpHeader = ({ children }: { children: React.ReactNode }) => (
  <header className="flex w-full  items-center mx-4">{children}</header>
);

export default SignUpHeader;
