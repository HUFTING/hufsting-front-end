import React from 'react';

const RegisterHeader = ({ children }: { children: React.ReactNode }) => (
  <header className="flex w-full  items-center">{children}</header>
);

export default RegisterHeader;
